/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    // async headers() {
    //     return [
    //         {
    //             source: '/:path*', // Apply these headers to all routes
    //             headers: [
    //                 {
    //                     key: 'Content-Security-Policy',
    //                     value: `
    //               default-src 'self';
    //             `.replace(/\s+/g, ' ').trim(), // Ensure it's a single-line string
    //                 },
    //             ],
    //         },
    //     ];
    // },
};

export default nextConfig;
